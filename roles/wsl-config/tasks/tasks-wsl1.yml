---

- name: "Set templates"
  template:
    src:   "etc/{{ item }}.j2"
    dest:  "/etc/{{ item }}"
    owner: root
    group: root
    mode:  '0644'
  with_items:
   - wsl.conf
   - hosts
  tags: wsl-config
